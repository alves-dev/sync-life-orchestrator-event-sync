name: publish

on:
  push:
    branches: main

jobs:
  build:
    uses: alves-dev/actions/.github/workflows/build-docker.yml@v1
    with:
      image-name: ghcr.io/alves-dev/synclife-orchestrator-event-sync